{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm run db:deploy && npm run db:backfill-photos && npm run db:backfill-blob-orphans && npm run build"
}
